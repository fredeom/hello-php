<?php if ($this->pageCount): ?>
    <nav>
        <ul class="pagination">
            <?php if (isset($this->previous)): ?>
                <li>
                    <a href="<?= $this->url($this->route, [], ['query'=>['page'=>$this->previous]]) . "&" . $this->filter; ?>" aria-label="Previous">
                        <span aria-hidden="true">&#10094;</span>
                    </a>
                </li>
            <?php else: ?>
                <li>
                        <span aria-hidden="true">&#10094;</span>
                </li>
            <?php endif; ?>
            <?php foreach ($this->pagesInRange as $page): ?>
                <?php if ($page != $this->current): ?>
                    <li>
                        <a href="<?= $this->url($this->route, [], ['query'=>['page'=>$page]]) . "&" . $this->filter; ?>"><?= $this->escapeHtml($page); ?></a>
                    </li>
                <?php else: ?>
                    <li>
                        <span aria-hidden="true"><b><?= $this->escapeHtml($page); ?></b></span>
                    </li>
                <?php endif; ?>
            <?php endforeach; ?>
            <?php if (isset($this->next)): ?>
                <li>
                    <a href="<?php echo $this->url($this->route, [], ['query'=>['page'=>$this->next]]) . "&" . $this->filter; ?>" aria-label="Next">
                        <span aria-hidden="true">&#10095;</span>
                    </a>
                </li>
            <?php else: ?>
                <li>
                    <span aria-hidden="true">&#10095;</span>
                </li>
            <?php endif; ?>
        </ul>
    </nav>
<?php endif; ?>
